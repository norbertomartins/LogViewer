<Window x:Class="LogViewer.App.Views.Dialogs.ExternalToolEditorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="External Tools"
        Width="680" Height="480"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Row="0" Grid.Column="0">
            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,6,0,0">
                <Button Content="Add" Width="60" Margin="0,0,6,0" Command="{Binding AddToolCommand}" />
                <Button Content="Remove" Width="60" Command="{Binding RemoveToolCommand}" CommandParameter="{Binding SelectedTool}" />
            </StackPanel>
            <ListBox ItemsSource="{Binding Tools}"
                     SelectedItem="{Binding SelectedTool, Mode=TwoWay}"
                     DisplayMemberPath="Name" />
        </DockPanel>

        <StackPanel Grid.Row="0" Grid.Column="2" DataContext="{Binding SelectedTool}">
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>

            <TextBlock Text="Name" />
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

            <TextBlock Text="Executable path" />
            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding ExecutablePath, UpdateSourceTrigger=PropertyChanged}" />
                <Button Grid.Column="1" Content="Browse..." Margin="6,0,0,0" Click="OnBrowseClick" />
            </Grid>

            <TextBlock Text="Argument template — {FilePath}, {LineNumber}, {LineText}" TextWrapping="Wrap" />
            <TextBox Text="{Binding ArgumentTemplate, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

            <TextBlock Text="Shortcut gesture (e.g. Ctrl+Alt+1) — optional" />
            <TextBox Text="{Binding ShortcutGesture, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

            <CheckBox Content="Auto-trigger when a line matches a highlight rule" IsChecked="{Binding AutoTriggerOnHighlightMatch}" Margin="0,0,0,6" />
            <ComboBox ItemsSource="{Binding DataContext.AvailableHighlightRules, RelativeSource={RelativeSource AncestorType=Window}}"
                      DisplayMemberPath="Name"
                      SelectedValuePath="Id"
                      SelectedValue="{Binding TriggerHighlightRuleId}"
                      IsEnabled="{Binding AutoTriggerOnHighlightMatch}"
                      Margin="0,0,0,8" />
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Width="80" Margin="0,0,6,0" Click="OnOkClick" IsDefault="True" />
            <Button Content="Cancel" Width="80" Click="OnCancelClick" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
