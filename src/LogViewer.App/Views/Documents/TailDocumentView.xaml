<UserControl x:Class="LogViewer.App.Views.Documents.TailDocumentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:LogViewer.App.Models"
             xmlns:controls="clr-namespace:LogViewer.App.Controls"
             xmlns:converters="clr-namespace:LogViewer.App.Converters">
    <UserControl.Resources>
        <DataTemplate x:Key="PlainLineTemplate" DataType="{x:Type models:LogLineViewModel}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="18" />
                    <ColumnDefinition Width="60" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding IsBookmarked, Converter={StaticResource BookmarkGlyphConverter}}" />
                <TextBlock Grid.Column="1" Text="{Binding LineNumber}" Opacity="0.6" />
                <TextBlock Grid.Column="2" Text="{Binding Text}" />
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="StructuredLineTemplate" DataType="{x:Type models:LogLineViewModel}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="18" />
                    <ColumnDefinition Width="60" />
                    <ColumnDefinition Width="90" />
                    <ColumnDefinition Width="70" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding IsBookmarked, Converter={StaticResource BookmarkGlyphConverter}}" />
                <TextBlock Grid.Column="1" Text="{Binding LineNumber}" Opacity="0.6" />
                <TextBlock Grid.Column="2" Text="{Binding Structured.Timestamp, StringFormat='HH:mm:ss.fff'}" Opacity="0.8" />
                <TextBlock Grid.Column="3" Text="{Binding Structured.Level}" FontWeight="Bold"
                           Foreground="{Binding Structured.Level, Converter={StaticResource LevelToBrushConverter}}" />
                <!-- Column 4: rendered message — colorized when the converter produces inlines,
                     plain text otherwise (no template, or colorization disabled). -->
                <TextBlock Grid.Column="4" Text="{Binding Structured.RenderedMessage}">
                    <controls:InlinesHelper.Source>
                        <MultiBinding Converter="{StaticResource StructuredInlinesConverter}">
                            <Binding Path="Structured" />
                            <Binding Path="DataContext.IsColorizeStructuredValues"
                                     RelativeSource="{RelativeSource AncestorType=UserControl}" />
                        </MultiBinding>
                    </controls:InlinesHelper.Source>
                </TextBlock>
            </Grid>
        </DataTemplate>

        <controls:LogLineTemplateSelector x:Key="LineTemplateSelector"
                                           PlainTemplate="{StaticResource PlainLineTemplate}"
                                           StructuredTemplate="{StaticResource StructuredLineTemplate}" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ToolBar Grid.Row="0" Background="{DynamicResource Theme.LogBackground}" Foreground="{DynamicResource Theme.LogForeground}">
            <ToggleButton Content="Follow Tail" IsChecked="{Binding IsFollowingTail}" />
            <ToggleButton Content="Structured View" IsChecked="{Binding IsStructuredView}" ToolTip="Parse this document as Serilog JSON (Time/Level/Message columns + property details)" />
            <!-- Colorize toggle: only meaningful while in structured view -->
            <ToggleButton Content="⬤ Colorize Values" IsChecked="{Binding IsColorizeStructuredValues}"
                          ToolTip="Highlight variable values in the message with distinct colors"
                          Visibility="{Binding IsStructuredView, Converter={StaticResource BoolToVisibilityConverter}}" />
            <TextBlock Text="Min Level:" VerticalAlignment="Center" Margin="8,0,4,0"
                       Visibility="{Binding IsStructuredView, Converter={StaticResource BoolToVisibilityConverter}}" />
            <ComboBox ItemsSource="{Binding LevelOptions}" SelectedItem="{Binding MinLevel}" Width="110"
                      ToolTip="Only show lines at or above this severity"
                      Visibility="{Binding IsStructuredView, Converter={StaticResource BoolToVisibilityConverter}}" />
            <Separator />
            <Button Content="Toggle Bookmark" Command="{Binding ToggleBookmarkCommand}" ToolTip="Ctrl+F2" />
            <Button Content="◀ Bookmark" Command="{Binding PreviousBookmarkCommand}" ToolTip="Shift+F2" />
            <Button Content="Bookmark ▶" Command="{Binding NextBookmarkCommand}" ToolTip="F2" />
            <Separator />
            <Button Content="◀ Highlight" Command="{Binding PreviousHighlightCommand}" ToolTip="Shift+F3" />
            <Button Content="Highlight ▶" Command="{Binding NextHighlightCommand}" ToolTip="F3" />
            <Separator />
            <Menu Background="Transparent">
                <MenuItem Header="Run Tool ▶" ItemsSource="{Binding ExternalTools}">
                    <MenuItem.ItemContainerStyle>
                        <Style TargetType="MenuItem">
                            <Setter Property="Header" Value="{Binding Name}" />
                            <Setter Property="Command" Value="{Binding RelativeSource={RelativeSource AncestorType=Menu}, Path=DataContext.RunExternalToolCommand}" />
                            <Setter Property="CommandParameter" Value="{Binding}" />
                        </Style>
                    </MenuItem.ItemContainerStyle>
                </MenuItem>
            </Menu>
            <Button Content="Search..." Command="{Binding SearchCommand}" ToolTip="Ctrl+Shift+F" />
            <Button Content="Customize..." Command="{Binding CustomizeCommand}" />
            <Separator />
            <Button Content="New lines below — resume follow" Command="{Binding ResumeFollowCommand}"
                    Visibility="{Binding HasUnseenChanges, Converter={StaticResource BoolToVisibilityConverter}}" />
        </ToolBar>

        <ListView Grid.Row="1" x:Name="LineListView"
                  ItemsSource="{Binding Lines}"
                  SelectedItem="{Binding SelectedLine, Mode=TwoWay}"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  VirtualizingPanel.ScrollUnit="Pixel"
                  Style="{StaticResource LogListViewStyle}"
                  FontFamily="Consolas" FontSize="12">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem" BasedOn="{StaticResource LogListViewItemStyle}">
                    <Setter Property="Background" Value="{Binding Background}" />
                    <Setter Property="Foreground" Value="{Binding Foreground}" />
                    <Setter Property="ContextMenu">
                        <Setter.Value>
                            <ContextMenu>
                                <MenuItem Header="Filter by TraceId"
                                          Command="{Binding ElementName=LineListView, Path=DataContext.FilterByTraceIdCommand}"
                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                <MenuItem Header="Filter by SpanId"
                                          Command="{Binding ElementName=LineListView, Path=DataContext.FilterBySpanIdCommand}"
                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                <Separator />
                                <MenuItem Header="Clear Filter"
                                          Command="{Binding ElementName=LineListView, Path=DataContext.ClearFilterCommand}" />
                            </ContextMenu>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplateSelector>
                <StaticResource ResourceKey="LineTemplateSelector" />
            </ListView.ItemTemplateSelector>
        </ListView>

        <!-- Structured-log detail: timestamp/template/properties/exception for the selected line, when it
             parsed as Serilog JSON. Collapses entirely for plain-text lines/documents. -->
        <Border Grid.Row="2" Background="{DynamicResource Theme.LogBackground}" TextElement.Foreground="{DynamicResource Theme.LogForeground}"
                BorderBrush="{DynamicResource Theme.BorderColor}" BorderThickness="0,1,0,0" Padding="8" MaxHeight="220">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SelectedLine.Structured}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel DataContext="{Binding SelectedLine.Structured}">
                    <TextBlock Text="{Binding Timestamp, StringFormat='Timestamp: {0:yyyy-MM-dd HH:mm:ss.fff zzz}'}" Margin="0,0,0,2" />
                    <TextBlock Text="{Binding MessageTemplate, StringFormat='Template: {0}'}" TextWrapping="Wrap" Margin="0,0,0,6"
                               Visibility="{Binding MessageTemplate, Converter={StaticResource NullOrEmptyToVisibilityConverter}}" />
                    <TextBlock Text="Properties" FontWeight="Bold" Margin="0,0,0,2" />
                    <ItemsControl ItemsSource="{Binding Properties}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,0,1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="160" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="Filter by this value"
                                                      Command="{Binding ElementName=LineListView, Path=DataContext.FilterByPropertyCommand}"
                                                      CommandParameter="{Binding}" />
                                        </ContextMenu>
                                    </Grid.ContextMenu>
                                    <TextBlock Grid.Column="0" Text="{Binding Key}" FontWeight="SemiBold" Margin="0,0,8,0" />
                                    <TextBlock Grid.Column="1" Text="{Binding Value}" TextWrapping="Wrap" />
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock Text="Exception" FontWeight="Bold" Margin="0,8,0,2"
                               Visibility="{Binding Exception, Converter={StaticResource NullOrEmptyToVisibilityConverter}}" />
                    <TextBox Text="{Binding Exception, Mode=OneWay}" IsReadOnly="True" TextWrapping="NoWrap"
                             FontFamily="Consolas" FontSize="11" AcceptsReturn="True" MaxHeight="120"
                             VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                             Visibility="{Binding Exception, Converter={StaticResource NullOrEmptyToVisibilityConverter}}" />
                </StackPanel>
            </ScrollViewer>
        </Border>

        <StatusBar Grid.Row="3" Background="{DynamicResource Theme.LogBackground}" Foreground="{DynamicResource Theme.LogForeground}">
            <StatusBar.Template>
                <ControlTemplate TargetType="StatusBar">
                    <Border Background="{TemplateBinding Background}">
                        <ItemsPresenter />
                    </Border>
                </ControlTemplate>
            </StatusBar.Template>
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}" />
            </StatusBarItem>
            <StatusBarItem Visibility="{Binding IsFilterActive, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding FilterStatusText}" Margin="0,0,8,0" />
                    <Button Content="✕ Clear filter" Command="{Binding ClearFilterCommand}" />
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>
